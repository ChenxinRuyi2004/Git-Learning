# 练习区

你可以在这个文件夹里随便创建、修改、删除文件，然后用 Git 练习：

```bash
git status
git add .
git commit -m "练习：修改 exercises"
```

推荐练习：

1. 新建一个 `day1.txt`，写下今天学到的 3 个命令
2. 提交一次
3. 再修改 `day1.txt`
4. 用 `git diff` 看改了什么
5. 再提交一次
6. 用 `git log --oneline --graph` 看提交历史

